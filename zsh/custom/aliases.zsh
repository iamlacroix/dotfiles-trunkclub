# reload shell config
alias reload='echo "Reloading shell with: exec -l $SHELL"; exec -l $SHELL'

# listing files
alias l='ls -F'
alias la='ls -FA'
alias ll='ls -lhF'
alias lla='ls -lhFA'
alias l.='ls -dF .*'
alias ll.='ls -dlhF .*'
